interface FightRecord {
    ko: {
        win: number
        loss: number
    }
    sub: {
        win: number
        loss: number
    }
    total: {
        win: number
        loss: number
    }
}
export type { FightRecord }